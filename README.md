# Handwritten image classification
It utilizes a 3-layer Artificial Neural Network (ANN) trained on the *MNIST dataset, achieving 97.9% accuracy. The model applies supervised Learning, recognizing patterns in pixel data, and improves generalization using regularization techniques like dropout layers, which boosted accuracy by 5% by reducing overfitting. 
